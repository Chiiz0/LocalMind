query previewProjectOfficeCommand($input: ProjectOfficeCommandInput!) {
  previewProjectOfficeCommand(input: $input) {
    artifactId
    expectedRevisionId
    packageFingerprint
    stateFingerprint
    stats
    summary
  }
}
