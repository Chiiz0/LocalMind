query projectResourceSources($projectId: String!, $resourceId: String!) {
  projectResourceSources(projectId: $projectId, resourceId: $resourceId) {
    workspaceId
    sourceResourceId
    title
    workspaceName
    sourceVersion
    projectVersion
  }
}
